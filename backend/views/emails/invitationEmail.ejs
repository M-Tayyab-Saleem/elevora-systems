<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Welcome to Elevora Systems</title>
  <style>
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background-color: #CDD9EA; /* Matches var(--color-bg-app) */
      margin: 0;
      padding: 0;
      -webkit-font-smoothing: antialiased;
    }
    .email-wrapper {
      width: 100%;
      background-color: #CDD9EA;
      padding: 40px 0;
    }
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      border-radius: 16px; /* var(--radius-xl) */
      overflow: hidden;
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    }
    .header {
      background-color: #ffffff;
      padding: 32px;
      text-align: center;
      border-bottom: 1px solid #e2e8f0;
    }
    .logo {
      height: 40px;
      width: auto;
    }
    .content {
      padding: 40px 32px;
      color: #1e293b; /* var(--color-text-primary) */
      text-align: center;
    }
    h1 {
      color: #0f172a; /* var(--color-text-heading) */
      font-size: 24px;
      font-weight: 700;
      margin-bottom: 16px;
      margin-top: 0;
    }
    p {
      font-size: 16px;
      line-height: 1.6;
      color: #475569; /* var(--color-text-secondary) */
      margin-bottom: 24px;
    }
    .btn-container {
      margin: 32px 0;
    }
    .btn {
      display: inline-block;
      background-color: #3b82f6; /* var(--color-brand-primary) */
      color: #ffffff;
      font-weight: 600;
      text-decoration: none;
      padding: 14px 32px;
      border-radius: 8px; /* var(--radius-md) */
      font-size: 16px;
      transition: background-color 0.2s;
    }
    .btn:hover {
      background-color: #2563eb;
    }
    .details-box {
      background-color: #F7FAFC; /* var(--color-bg-primary) */
      border: 1px solid #e2e8f0;
      border-radius: 12px;
      padding: 20px;
      margin-top: 24px;
      text-align: left;
    }
    .detail-row {
      display: flex;
      margin-bottom: 8px;
      font-size: 14px;
    }
    .detail-label {
      font-weight: 600;
      width: 100px;
      color: #64748b;
    }
    .detail-value {
      color: #1e293b;
    }
    .footer {
      background-color: #f8fafc;
      padding: 24px;
      text-align: center;
      font-size: 12px;
      color: #94a3b8; /* var(--color-text-muted) */
      border-top: 1px solid #e2e8f0;
    }
  </style>
</head>
<body>
  <div class="email-wrapper">
    <div class="email-container">
      <div class="header">
        <img src="https://placehold.co/180x60?text=Elevora+Systems" alt="Elevora Systems" class="logo">
      </div>
      
      <div class="content">
        <h1>Welcome to the Team!</h1>
        <p>Hello <strong><%= name %></strong>,</p>
        <p>An account has been created for you at Elevora Systems. We are excited to have you on board.</p>
        
        <div class="details-box">
          <div class="detail-row">
            <span class="detail-label">Role:</span>
            <span class="detail-value"><%= role %></span>
          </div>
          <div class="detail-row">
            <span class="detail-label">Email:</span>
            <span class="detail-value"><%= email %></span>
          </div>
        </div>

        <p>You can now log in to your workspace.</p>

        <div class="btn-container">
          <a href="<%= loginURL %>" class="btn">Login to Elevora Systems</a>
        </div>
        
        <p style="font-size: 14px; color: #64748b;">
          If the button doesn't work, copy and paste this link into your browser:<br>
          <a href="<%= loginURL %>" style="color: #3b82f6;"><%= loginURL %></a>
        </p>
      </div>

      <div class="footer">
        <p>&copy; <%= new Date().getFullYear() %> Elevora Systems. All rights reserved.</p>
        <p>This is an automated message, please do not reply.</p>
      </div>
    </div>
  </div>
</body>
</html>